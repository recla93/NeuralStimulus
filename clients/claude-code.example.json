{
  "_comment": "Claude Code — place as .mcp.json in project root, or merge into ~/.claude.json",
  "mcpServers": {
    "neuron": {
      "command": "python3",
      "args": ["-m", "neuron"],
      "cwd": "%LOCALAPPDATA%\\Programs\\neuron"
    }
  }
}
